---
description: Classify decisions as one-way or two-way doors and determine required analysis depth
argument-hint: [--interactive] <decision>
---

Invoke the `doors` skill to analyze the user's input.

If the user includes `--interactive` in their arguments, use Interactive Mode as described in the skill. Otherwise, use Structured Mode.

The user's input to analyze: $ARGUMENTS
